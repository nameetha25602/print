#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    
    int num;
    scanf("%d", &num);
    
    if (num % 3 == 0 && num % 5 == 0)
        printf("HiHello\n");
    else if (num % 3 == 0)
        printf("Hi\n");
    else if (num % 5 == 0)
        printf("Hello\n");
    
        /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
