int main() 
{
    int num;
    scanf("%d",&num);
    if (num > 0)
       printf("Positive\n");
    else if (num < 0) 
       printf("Negative\n");
    else
       printf("Zero\n");

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
